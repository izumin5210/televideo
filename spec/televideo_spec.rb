require "spec_helper"

describe Televideo do
  it "has a version number" do
    expect(Televideo::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
